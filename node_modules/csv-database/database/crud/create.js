const create = async (parser, data) => {
    const arrayData = data instanceof Array ? data : [data];
    if (arrayData.length < 1)
        return arrayData;
    await parser.add(arrayData);
    return arrayData;
};
module.exports = create;
