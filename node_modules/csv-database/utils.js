const isSubsetOf = (subset, obj) => {
    for (const key of Object.keys(subset)) {
        if (subset[key] !== obj[key]) {
            return false;
        }
    }
    return true;
};
module.exports = {
    isSubsetOf
};
